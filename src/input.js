class input{
    constructor(parent, inputSchema){
        this.parent = parent
        this.inputSchema = inputSchema
        var parentElement = document.getElementById(parent)
        parentElement.innerHTML = "test"
    }

}